import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import CloudStream from "./component";

ReactDOM.render(
    <React.StrictMode>
        <CloudStream/>
    </React.StrictMode>,
    document.getElementById('root')
);